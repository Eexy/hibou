---
import Tag from './tag.astro';

const { tags } = Astro.props;
---

{
  tags && (
    <ul class="flex gap-x-4 flex-wrap gap-y-4 py-2">
      {tags.map((tag: string) => (
        <Tag tag={tag} />
      ))}
    </ul>
  )
}
