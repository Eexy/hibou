---
import Tag from './tag.astro';

const { tags } = Astro.props;
---

{
  tags && (
    <ul class="flex gap-x-4">
      {tags.map((tag: string) => (
        <Tag tag={tag} />
      ))}
    </ul>
  )
}
